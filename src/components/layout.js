import React from "react";

const Layout = () => {
  return (
    <div
      style={{
        height: 600,
        width: 400,
        border: "1px solid black",
        borderRadius: 10,
      }}
    ></div>
  );
};

export default Layout;
