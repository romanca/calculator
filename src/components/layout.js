import React from "react";
import Screen from "./screen";

const Layout = () => {
  return (
    <div
      style={{
        height: 600,
        width: 400,
        border: "1px solid black",
        borderRadius: 10,
      }}
    >
      <Screen />
      fwef
    </div>
  );
};

export default Layout;
